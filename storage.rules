rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Praxis-Dokumente: nur authentifizierte Nutzer
    match /praxen/{praxisId}/patienten/{patientId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
